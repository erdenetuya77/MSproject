# 🚀 Төслийн суулгалт ба ажиллуулах заавар

Энэ заавар нь **Зээлийн эрсдэлийн үнэлгээ - Naive Bayes** төслийг анхнаас эхлэн суулгаж, ажиллуулах бүрэн гарын авлага юм.

---

## 📋 Урьдчилсан шаардлага

Доорх программуудыг урьдчилан суулгасан байх ёстой:

- **Python 3.9+** - [Татаж авах](https://www.python.org/downloads/)
- **Git** - [Татаж авах](https://git-scm.com/downloads)
- **Quarto** - [Татаж авах](https://quarto.org/docs/get-started/)

### Суулгасан эсэхийг шалгах:

```bash
python3 --version   # Python 3.9.0 эсвэл дээш хувилбар гарах ёстой
git --version       # git version 2.x.x гарах ёстой
quarto --version    # 1.3.0 эсвэл дээш хувилбар гарах ёстой
```

---

## 📂 1-р алхам: Төслийн файлууд

Таны төслийн бүтэц дараах байдалтай байх ёстой:

```
MSproject/
├── data/
│   └── credit_risk_dataset.csv      # Өгөгдлийн файл (1.8 MB)
├── src/
│   ├── preprocessing.py             # Өгөгдөл боловсруулалт
│   └── models.py                    # Naive Bayes загвар
├── notebook/
│   └── analysis.ipynb               # Jupyter notebook шинжилгээ
├── outputs/                         # Үр дүнгийн график хадгалах folder
├── report.qmd                       # Quarto тайлан
├── references.bib                   # Эх сурвалж
├── ieee.csl                         # Citation стайл
├── requirements.txt                 # Python сангууд
├── README.md                        # Төслийн тайлбар
├── SETUP.md                         # Энэ заавар
```

### Шалгах:

```bash
cd /Users/edi/Documents/Prob-Stat/MSproject
ls -la
```

✅ Хэрэв дээрх бүх файлууд харагдаж байвал бэлэн.

---

## 🐍 2-р алхам: Python Virtual Environment үүсгэх

Virtual environment нь таны төслийн сангуудыг бусад төслүүдээс тусгаарлан хадгална.

```bash
# Төслийн үндсэн директор руу орох
cd /Users/edi/Documents/Prob-Stat/MSproject

# Virtual environment үүсгэх
python3 -m venv venv

# Идэвхжүүлэх (Mac/Linux)
source venv/bin/activate

# ЭСВЭЛ Windows дээр:
# venv\Scripts\activate
```

✅ Амжилттай болвол терминал дээр `(venv)` гэж гарна:

```
(venv) edi@MacBook MSproject %
```

---

## 📦 3-р алхам: Python сангууд суулгах

```bash
# Virtual environment идэвхтэй байх ёстой!
pip install --upgrade pip

# Бүх шаардлагатай сангууд суулгах
pip install -r requirements.txt
```

### Суулгагдах сангууд:

- numpy (1.24.0+)
- pandas (2.0.0+)
- scikit-learn (1.3.0+)
- matplotlib (3.7.0+)
- seaborn (0.12.0+)
- jupyter (1.0.0+)
- ipykernel (6.25.0+)
- scipy (1.11.0+)
- PyYAML (6.0+)

### Шалгах:

```bash
pip list | grep -E "numpy|pandas|scikit-learn"
```

✅ Дээрх сангууд жагсаалтанд байвал амжилттай.

---

## 📄 4-р алхам: Quarto болон TinyTeX суулгах

PDF тайлан үүсгэхийн тулд TinyTeX (LaTeX) шаардлагатай.

```bash
# TinyTeX суулгах
quarto install tinytex
```

Энэ нь 5-10 минут үргэлжилж болно. Уншсаны дараа:

```bash
# Шалгах
quarto check
```

✅ "LaTeX: OK" гарвал бэлэн.

---

## 🔬 5-р алхам: Jupyter Notebook ажиллуулах

### А. Jupyter-ийг эхлүүлэх:

```bash
# Virtual environment идэвхтэй байх ёстой!
cd /Users/edi/Documents/Prob-Stat/MSproject

jupyter notebook
```

Таны browser автоматаар нээгдэнэ (`http://localhost:8888`).

### Б. Notebook нээх:

1. `notebook/` folder руу орох
2. `analysis.ipynb` дарж нээх

### В. Бүх cell-үүдийг ажиллуулах:

**Арга 1**: Navbar-аас `Cell` → `Run All`

**Арга 2**: Дараалан `Shift + Enter` дарах

### Г. Хүлээгдэж буй үр дүн:

```
📂 Төслийн үндсэн директор: /Users/edi/Documents/Prob-Stat/MSproject
📂 Код: /Users/edi/Documents/Prob-Stat/MSproject/src
📂 Өгөгдөл: /Users/edi/Documents/Prob-Stat/MSproject/data
📂 Гаралт: /Users/edi/Documents/Prob-Stat/MSproject/outputs

✅ Бүх модулууд амжилттай импортлогдлоо
✅ Өгөгдлийн файл олдлоо
   Зам: /Users/edi/Documents/Prob-Stat/MSproject/data/credit_risk_dataset.csv
   Хэмжээ: 1.72 MB

⏳ Өгөгдлийг боловсруулж байна...

БЭЛЭН БОЛСОН ӨГӨГДӨЛ
============================================================
  Train багц: 26,064 өгөгдөл, 25 шинж чанар
  Test багц:  6,517 өгөгдөл, 25 шинж чанар
  Feature-үүд: 25 ширхэг
============================================================

⏳ Загварыг сургаж байна...
✓ Naive Bayes загвар сургагдлаа

NAIVE BAYES ЗАГВАРЫН ҮР ДҮН
=====================================================================
             Accuracy  Precision    Recall  F1-Score       AUC
Naive Bayes   0.8328     0.6536    0.4316    0.5197    0.8630
=====================================================================

✅ График хадгалагдлаа: /Users/edi/Documents/Prob-Stat/MSproject/outputs/confusion_matrix.png
✅ График хадгалагдлаа: /Users/edi/Documents/Prob-Stat/MSproject/outputs/roc_curve.png
✅ График хадгалагдлаа: /Users/edi/Documents/Prob-Stat/MSproject/outputs/performance_metrics.png

🎉 Notebook дууслаа! Бүх график outputs/ folder дотор хадгалагдсан.
```

### Д. Хадгалагдсан графикуудыг харах:

```bash
open outputs/
# ЭСВЭЛ Windows дээр: explorer outputs\
```

Дараах 3 график үүсэх ёстой:
- `confusion_matrix.png`
- `roc_curve.png`
- `performance_metrics.png`

---

## 📊 6-р алхам: PDF тайлан үүсгэх

Quarto-г ашиглан `report.qmd`-аас PDF үүсгэх:

```bash
cd /Users/edi/Documents/Prob-Stat/MSproject

# PDF render хийх
quarto render report.qmd
```

### Үргэлжлэх хугацаа:

Анхны удаад **5-10 минут** үргэлжилнэ (Python кодууд ажиллах, зурагууд үүсгэх, LaTeX compile хийх).

### Хүлээгдэж буй үр дүн:

```
processing file: report.qmd
  |..........................................| 100%

Output created: report.pdf
```

✅ `report.pdf` файл үүсэх ёстой:

```bash
ls -lh report.pdf
# -rw-r--r--  1 edi  staff   2.1M Dec  5 10:30 report.pdf
```

### PDF-г нээх:

```bash
open report.pdf
# ЭСВЭЛ Windows дээр: start report.pdf
```

---

## ⚠️ Түгээмэл алдаа ба шийдэл

### ❌ Алдаа 1: `ModuleNotFoundError: No module named 'preprocessing'`

**Шалтгаан**: Virtual environment идэвхгүй эсвэл сангууд суулгаагүй.

**Шийдэл**:
```bash
source venv/bin/activate  # Virtual env идэвхжүүлэх
pip install -r requirements.txt
```

---

### ❌ Алдаа 2: `FileNotFoundError: '../data/credit_risk_dataset.csv'`

**Шалтгаан**: Өгөгдлийн файл байхгүй эсвэл зам буруу.

**Шийдэл**:
```bash
# Файл байгаа эсэхийг шалгах
ls -lh data/credit_risk_dataset.csv

# Хэрэв файл байхгүй бол:
# Kaggle-аас татаж авна уу: https://www.kaggle.com/datasets/laotse/credit-risk-dataset
# data/ folder дотор хадгална
```

---

### ❌ Алдаа 3: `TeX not found` (Quarto render)

**Шалтгаан**: TinyTeX суулгаагүй.

**Шийдэл**:
```bash
quarto install tinytex
quarto check
```

---

### ❌ Алдаа 4: Монгол үсэг буруу харагдах (PDF дээр)

**Шалтгаан**: Font асуудал.

**Шийдэл**: `report.qmd` дээр font солих (одоо `Noto Sans Mongolian` ашиглаж байна, хэрэв танд байхгүй бол `Arial Unicode MS` эсвэл `DejaVu Sans` ашиглаж болно).

---

### ❌ Алдаа 5: Jupyter notebook notebook/ folder-аас ажиллахгүй байна

**Шалтгаан**: Working directory алдаатай.

**Шийдэл**: Notebook-д автомат path detection бичигдсэн, гэхдээ алдаа гарвал төслийн үндсэн директороос Jupyter-г эхлүүлээрэй:

```bash
cd /Users/edi/Documents/Prob-Stat/MSproject
jupyter notebook notebook/analysis.ipynb
```

---

## ✅ Шалгалтын checklist

Бүх зүйл зөв ажиллаж байгаа эсэхийг шалгах:

- [ ] Python 3.9+ суусан (`python3 --version`)
- [ ] Quarto суусан (`quarto --version`)
- [ ] Virtual environment үүсгэсэн (`venv/` folder байгаа)
- [ ] Сангууд суулгасан (`pip list | grep pandas`)
- [ ] TinyTeX суулгасан (`quarto check`)
- [ ] Jupyter notebook ажилласан (графикууд outputs/ дотор байгаа)
- [ ] PDF үүссэн (`report.pdf` файл байгаа)

---

## 🎯 Дэс дараалал (Хурдан хувилбар)

Хэрэв та бүх зүйлийг анхнаас эхлэн хийх бол:

```bash
# 1. Төсөл руу орох
cd /Users/edi/Documents/Prob-Stat/MSproject

# 2. Virtual env үүсгэж идэвхжүүлэх
python3 -m venv venv
source venv/bin/activate

# 3. Сангууд суулгах
pip install --upgrade pip
pip install -r requirements.txt

# 4. TinyTeX суулгах (анхны удаа)
quarto install tinytex

# 5. Jupyter ажиллуулах
jupyter notebook notebook/analysis.ipynb
# → Browser дээр Cell → Run All дарах
# → Графикууд outputs/ folder дотор хадгалагдана

# 6. PDF үүсгэх
quarto render report.qmd
# → report.pdf үүснэ

# 7. Үр дүн харах
open report.pdf
open outputs/
```

---

## 📞 Тусламж

Хэрэв алдаа гарвал:

1. **Алдааны мессежийг** анхааралтай уншина уу
2. **Дээрх "Түгээмэл алдаа" хэсгээс** шийдлийг хайна уу
3. **Quarto documentation**: https://quarto.org/docs/guide/
4. **Scikit-learn guide**: https://scikit-learn.org/stable/user_guide.html

---

## 🎓 Төслийн бүрэн судалгаа үзэх дараалал

1. `README.md` - Төслийн тойм
2. `SETUP.md` - Суулгалт (энэ файл)
3. `notebook/analysis.ipynb` - Jupyter шинжилгээ
4. `report.pdf` - Бүрэн тайлан (Quarto-аас үүсгэсэн)
5. `src/preprocessing.py` - Өгөгдөл боловсруулалтын код
6. `src/models.py` - Naive Bayes загварын код

---

**✨ Амжилт хүсье!**

Хэрэв дээрх заавар дагаж ажиллуулахад асуудал гарвал файлуудын байршил, Python хувилбар, эсвэл virtual environment-ын асуудал байж болзошгүй.
